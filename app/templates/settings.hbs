<h4>Settings</h4>
<div>
  <LinkTo @route="flows" @models={{array "slug-1111" "subscription" "change"}}>Go to flow</LinkTo> (linkTo with a @models array)
</div>
<div>
  <LinkTo @route="flows" @model={{hash organization_slug="slug-1111" name="subscription" step_id="change"}}>Go to flow</LinkTo> (linkTo with a @model hash)
</div>

<ul>
  <li>paramNames: {{this.paramsNamesAsString}}</li>
  <li>params: {{this.paramsAsString}}</li>
</ul>